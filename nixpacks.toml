# Railway nixpacks configuration
# Ensures Playwright's Chromium has all required system dependencies

[phases.setup]
nixPkgs = [
    "python39",
    "chromium"
]

[phases.install]
cmds = [
    "pip install -r requirements.txt"
]

[start]
cmd = "./start.sh"
