# Nixpacks configuration for Railway
# Let Nixpacks auto-detect Python and dependencies
# Only override the start command to run migrations first

[start]
cmd = "alembic upgrade head && uvicorn app.main:app --host 0.0.0.0 --port $PORT"
